import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'kurkudos.lv',
  appName: 'Kurkudos',
  webDir: 'dist/apps/cards',
  bundledWebRuntime: false
};

export default config;
